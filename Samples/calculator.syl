print("Enter a term: ")
string term = readLine()
var i = 0
sylph("i = " + term) // sylph-function can parse and execute SylphScript with variables, functions and types at the current position!
print("The result is: " + i)
readKey()